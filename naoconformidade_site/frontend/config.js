// Arquivo: frontend/config.js
// Aqui você centraliza a URL do backend (API) usada pelo frontend

const CONFIG = {
  API_URL: "https://naoconformidade-1.onrender.com", // <-- coloque aqui a URL pública do Render do backend
};

export default CONFIG;
